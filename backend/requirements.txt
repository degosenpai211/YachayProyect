# Nota: cualquier edicion a este archivo fuerza a Railway/Railpack a
# reinstalar dependencias en vez de reusar una capa de build en cache.
# bump-deploy: 2026-07-26-v7 (TTS ElevenLabs + audio Zavu)
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic==2.10.4
pydantic-settings==2.7.0
httpx==0.28.1
python-multipart==0.0.20
anthropic==0.42.0
# Driver de Postgres — necesario solo si DATABASE_URL apunta a Postgres
# (Railway Postgres, Supabase, etc). No afecta el uso local con SQLite.
psycopg2-binary==2.9.10
